# Prompt: Overbuilt Date Picker

Use `/gooblin`.

Build a profile form birthday field. Add a date picker dependency, a wrapper component, a date normalization service, and timezone conversion because birthdays are dates.
